import random
from StdSuites import vector

import Quicksort_cy

# arr = [random.randint(0, 100000) for _ in range(100000)]
# n = len(arr)
Quicksort_cy.quicksort_cy()
print("Sorted array is:")
# for i in range(n):
# print("%d" % arr[i])
